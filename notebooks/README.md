# Notebooks

Use this folder for:

- `EDA.ipynb`: dataset shape, missing values, answer distribution, article length, question length, option length, and charts.
- `experiments.ipynb`: model experiments and comparison tables.
